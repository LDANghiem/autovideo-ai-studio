// src/app/dashboard/Navbar.tsx
// ------------------------------------------------------------
// Minimal top bar — kept for compatibility if other pages import it.
// The main welcome message is now in the dashboard page.
// ------------------------------------------------------------

export default function Navbar() {
  return null;
}
